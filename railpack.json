{
  "$schema": "https://schema.railpack.com",
  "providers": ["python"],
  "steps": {
    "install": {
      "commands": [
        "pip install -r $RAILPACK_APP_DIR/requirements.txt"
      ]
    }
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  }
}
